// Generated by CoffeeScript 1.9.3
(function() {
  $(document).ready(function() {
    var socket;
    socket = io();
    socket.on('connect', function() {
      return console.log('connected');
    });
    socket.on('anim response', function(msg) {
      return console.log(msg);
    });
    $('.menu-item').click(function() {
      var mapping, menu_id;
      mapping = {
        'bg-btn': 'bg-menu',
        'fg-btn': 'fg-menu',
        'color-btn': 'color-menu'
      };
      menu_id = '#' + mapping[$(this).attr('id')];
      $(menu_id).show();
      return $('#main-menu').hide();
    });
    $('.anim-selector').click(function() {
      var splitted;
      splitted = $(this).attr('id').split('-');
      socket.emit('anim request', {
        'anim_type': splitted[0],
        'anim_id': splitted[1]
      });
      $(this).parent().hide();
      return $('#main-menu').show();
    });
    return $('.menu-item').each(function() {
      return $(this).css({
        content: 'url(../images/' + $(this).attr('id') + '-up.png)'
      });
    });
  });

}).call(this);
